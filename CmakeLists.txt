cmake_minimum_required(VERSION 3.0.0)
set(CMAKE_CXX_STANDARD 17)
project(leetgo VERSION 0.1.0)

include(CTest)
enable_testing()

# 源文件
aux_source_directory(cpp leetcom)

# 头文件
include_directories(cpp)

add_executable(${PROJECT_NAME} ${leetcom} ${leetcn})

